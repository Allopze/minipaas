# Environment file for MiniPaaS
# Required: secret used to sign JWT tokens
JWT_SECRET=test-secret-key-123

# Optional: server port (default 5050)
PORT=5050

# Rate limiting: window in minutes and max attempts per window
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_ATTEMPTS=5

# CORS: comma-separated list of allowed origins (leave empty for localhost only)
# Example: CORS_ORIGINS=https://myapp.com,https://admin.myapp.com
CORS_ORIGINS=

# Auto-restart policy: max restarts within window before giving up
AUTO_RESTART_MAX=3
AUTO_RESTART_WINDOW=300

# Log rotation: max size in MB before rotation
LOG_MAX_SIZE_MB=10
LOG_MAX_FILES=5
